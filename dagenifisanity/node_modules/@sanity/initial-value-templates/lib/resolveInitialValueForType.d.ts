import { ArraySchemaType, InitialValueParams, SchemaType } from '@sanity/types';
export declare function getItemType(arrayType: ArraySchemaType, item: unknown): SchemaType | undefined;
/**
 * Resolve initial value for the given schema type (recursively)
 *
 * @param type {SchemaType} this is the name of the document
 * @param params {Record<string, unknown>} params is a sanity context object passed to every initial value function
 * @param maxDepth {Record<string, unknown>} maximum recursion depth (default 9)
 */
export declare function resolveInitialValueForType(type: SchemaType, params?: InitialValueParams, maxDepth?: number): any;
//# sourceMappingURL=resolveInitialValueForType.d.ts.map